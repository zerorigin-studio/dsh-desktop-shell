# Changelog

## 0.5.0 (2026-09-18)

- **设置页支持深浅色主题**（捆绑客户端 → 0.5.0）：新增「外观」设置，三选一——**跟随系统**（默认）/ **浅色** / **深色**，切换立即生效。
  - 配色令牌改为「浅色为基线 + `html.dark` 覆盖 + `prefers-color-scheme` 跟随」三层结构，同一套变量两套取值；浅色与深色各自成块，便于对照维护。
  - 原先把颜色硬编码在规则里的位置（开关滑块、分段控件选中态、版本号文字色、页脚等）统一收进令牌，避免深色值泄漏到浅色。
  - `html.light` 单独写一份覆盖块：否则在「系统为深色」的机器上选浅色会被媒体查询抢走变量，出现"选了浅色仍是深色"。
  - 首帧前用缓存的偏好预设主题（内联脚本 + localStorage），消除"先渲染深色再闪成浅色"。
  - 系统偏好变化时，处于跟随模式的窗口会即时刷新提示文案。
  - 设置项 `theme`（`system`/`light`/`dark`），非法值回退 `system`。

## 0.4.3 (2026-09-18)

- **设置页布局调整**（捆绑客户端 → 0.4.3），按反馈修正三处：
  - 标签栏由左侧竖向导航改回**横向排布**，放在内容上方；
  - 移除顶部品牌区（logo、「设置」标题、「DSH 桌面客户端」副标题），只保留标签栏与右侧连接状态胶囊，纵向空间还给内容；
  - **状态区改为按页归属**：原先是一个跨页常驻的公共状态条，导致在「运行时」页查询出的「发现 3 个可用版本」会跟着用户切到其它页。现在「运行时」与「组件」两页各带一个独立状态区，消息与进度只留在触发它的那一页；「通用」「关于」没有异步操作，不再显示任何操作信息。轮询期间即使用户切走，进度也只更新发起操作的那一页。

## 0.4.2 (2026-09-18)

- **设置页视觉重做**（捆绑客户端 → 0.4.2）：对齐 WorkBuddy Manager 那套 shadcn/ui 设计语言——中性色阶配单一强调色、发丝边框（1px 低对比描边）、以 `--radius` 为基准的圆角分层、半透明叠加表达选中态。
  - 顶部改为品牌栏 + 连接状态胶囊（运行中用绿点呼吸，未运行红点），Harness 状态不再埋在「关于」页里；
  - 标签栏由横向按钮改为**左侧竖向导航**（带图标），与内容区形成明确的主从结构；
  - 卡片式分组：每组设置一张卡，组内条目以分隔线切分，不再是一排排独立圆角块；
  - 开关改为细轨道 + 圆形滑块，单选改为分段控件（segmented control），选中态用卡片底色而非高亮描边；
  - 统一留白节奏与字号层级（13px 正文 / 11px 辅助 / 10.5px 页脚），并补上深色滚动条样式；
  - 设置窗口 660×720 → **720×740**（最小 600×540），给侧栏导航留出横向空间。
- 功能与行为完全不变，仅视觉与布局重做。

## 0.4.1 (2026-09-18)

- **设置页改为标签页布局**（捆绑客户端 → 0.4.1）：原先所有设置挤在一页纵向滚动，改动后按功能分为四个标签页：
  - **通用**：开机自启、关闭主窗口行为、退出时是否保留 Harness；
  - **运行时**：存放位置、已下载版本（切换/删除）、从发布仓库查询并下载新版本；
  - **组件**：可选组件的安装与卸载（0.4.0 新增的能力）；
  - **关于**：客户端/Harness/插件版本、Harness 状态与连接地址、工作区。
- 进度条与状态提示移到标签栏下方的**常驻区域**——无论在哪个标签页，运行时下载、版本切换重启、组件安装的进度都可见。
- 组件页数据改为**首次进入时才加载**（它可能触发一次运行时清单补拉），不再拖慢设置页启动。
- 设置窗口由固定 640×860 改为 660×720 且**允许缩放**（最小 560×520）：标签页之间内容长度差异大，固定高度会憋住运行时页。

## 0.4.0 (2026-09-16)

- **可选组件按需安装**（捆绑客户端 → 0.4.0）。新增能力：运行时可以只带基础部分，大体积依赖作为「可选组件」在设置页按需安装。
  - 背景：官方 0.1.6 起引入 Office→PDF 能力，随之带上 `@deepseek-ai/libreoffice-kit-win32-x64`（解压 325MB，占整包 79%）。整包因此涨到 163.8MB，超出 Gitee 单附件 100MB 上限。基础 runtime 现已剔除该组件（**52.4MB**），harness 与 Web UI 完整可用。
  - 组件来源是**官方公开发布的 npm 包**，客户端直接走 npm 镜像下载（实测 npmmirror 33MB/s），用 npm 元数据里的 **sha512 integrity** 校验，不需要自行托管、不依赖系统 npm。
  - 设置页新增「可选组件」区块：显示组件名/体积/安装状态，一键安装或卸载；下载与安装进度复用运行时那套进度通道。
  - 安装采用 staging + 原子改名，校验（sha512 + 包名/版本比对）失败不会留下半个组件。
  - 运行时清单（manifest）会随 runtime 一并留档（`dsh-runtime-manifest.json`），组件声明（包名/版本/integrity/体积/安装位置）写在 `optional` 段；未声明组件的旧 runtime 会明确提示"该运行时未记录组件信息"。
- **修复**：组件安装目录拼接错误——`InstallTo` 已含 scope 目录，直接用完整包名会拼出 `@deepseek-ai/@deepseek-ai/...` 的重复层级。已由单元测试覆盖。

## 0.3.3 (2026-09-16)

- **修复：设置页选中的运行时版本被忽略**（捆绑客户端 → 0.3.3）。此前客户端启动时，"协议文件里的 `runtimeDir`" 优先于用户选中的版本——在源码树 / 插件环境下会表现为：明明下载并选中了新版本（如 0.1.6-alpha.1），实际跑的仍是协议文件指向的那份（如源码树的 0.1.5-rc.2），下载的版本永远轮不到。现在**只要用户在设置页选过版本（`active.json` 存在），就优先使用它**，协议文件的 `runtimeDir` 退为兜底。
- **修复：切换版本无法自动重启 harness**。归属判断原先依赖协议文件的 `owner` 字段，但 dsh 启动后加载插件，插件会把自己的记录写进同一个文件（`owner=plugin`）覆盖客户端写的 `owner=client`，客户端因此永远不敢重启。现在客户端在 `%LOCALAPPDATA%\dsh-desktop-shell-client\harness-state.json` 独立记录自己拉起的 harness（pid / 版本 / 路径 / 地址），不再受共享文件被覆盖影响。
- 说明：要让已下载的新版本真正生效，仍需当前旧的 harness 进程结束一次（退出客户端时关闭「保留 Harness」，或让它自然退出）。

## 0.3.2 (2026-09-13)

- **切换运行时版本即时生效**（捆绑客户端 → 0.3.2）：此前设置页的「使用」只写 `active.json`，必须重启整个客户端才生效。现在若正在运行的 harness 是客户端自己拉起的（协议文件 `owner=client`），切换后客户端会就地停掉旧实例、用所选版本重启，并把窗口切到新实例。插件拉起的 harness 不属于客户端，**绝不擅自杀掉**，仍只写 `active.json` 并提示重启。
- **设置页「关于」显式对照版本**：Harness 版本标注「（运行中）」，与已选版本不一致时提示"重启 Harness 后生效"，避免看起来像切换失败。
- 切换与重启期间复用同一进度通道，显示「正在用 <版本> 重启 Harness…」。

## 0.3.1 (2026-09-13)

- **runtime 存放位置按形态分流**（捆绑客户端 → 0.3.1）：
  - *插件形态*（exe 由 dsh 插件体系安装，位于 `<DSH_HOME>/profiles/<profile>/node_modules/<pkg>/bin/`）：runtime 用默认位置 `%LOCALAPPDATA%\dsh-desktop-shell-client\runtime\`。那类目录归包管理器（pnpm）所有，升级插件会替换整个包目录，放在旁边的 runtime 会被一起清掉，等于每次升级重下 200MB+。
  - *独立客户端形态*：runtime 放在 exe 同目录的 `runtime\` 下，「客户端 + runtime」成为一个可整体拷贝、可离线的自包含目录。
  - 新增设置项 `runtimeLocation`：`auto`（默认，按形态）/ `portable`（随客户端）/ `user`（默认位置）；环境变量 `DSH_SHELL_RUNTIME_DIR` 优先级最高。
- **设置页新增「运行时」区块**：显示存放位置与实际路径、已下载版本列表（可切换为当前版本、可删除），并从发布仓库匿名查询可下载版本、一键下载（带进度）。WebView2 用户数据与 harness 日志位置不变（固定在 `%LOCALAPPDATA%\dsh-desktop-shell-client\`）。
- **修复**：runtime 目录从 `%LOCALAPPDATA%\dsh-desktop-shell\` 挪到 `dsh-desktop-shell-client\`。前者是更早版本客户端遗留的 WebView2 用户数据目录（实测 3200+ 文件 / 500MB+ 浏览器 profile），把 runtime 混进去容易在清理浏览器数据时被一并删除。

## 0.3.0 (2026-09-13)

- **客户端可独立运行（新能力）**：捆绑客户端（bin/dsh-desktop-shell-client.exe → 0.3.0）不再要求机器上先有 dsh。本机没有可用 harness 时，客户端会从发布仓库（Gitee `coldcgh/dsh-runtime`）下载官方 `@deepseek-ai/dsh` 运行时，校验 sha256、解压到 `%LOCALAPPDATA%\dsh-desktop-shell\runtime\<version>`，再自行拉起 harness。下载走匿名直链，客户端不含任何凭证。于是同一个 exe 覆盖两种形态：
  - **插件形态**（本插件已装、dsh 在跑）：协议文件可达 → 直接附着，行为与 0.2.9 一致；
  - **独立形态**（裸机）：没有 harness → 下载运行时 → 启动 → 附着。
  - 加载页会显示下载进度（含百分比），失败显示具体原因；
  - 设置项新增 `pinnedDshVersion`（锁定运行时版本，留空自动选择）。
- **协议文件新增 `owner` 字段**：插件写 `owner=plugin`，独立客户端写 `owner=client`。插件卸载（dispose）时只删除自己写的那份，不再误删独立形态留下的状态。历史文件没有该字段时按插件所有处理，向后兼容。
- **修复**：客户端工作区不再硬编码 `D:\WorkSpaces\DeepSeekHerness`。改为「环境变量 > 设置项 > 历史目录（仅当存在，用于老用户无损迁移）> ~/DeepSeekHarness」。原硬编码会让分发给其它机器的用户把会话写到一个不存在的路径。
- **修复**：客户端与 harness 的数据根目录现在统一按 `DSH_HOME`（缺省 `~/.dsh`）解析。此前客户端固定用 `~/.dsh`，在设置了 `DSH_HOME` 的环境里会与 harness 各写各的目录。
- **改进**：harness 启动日志落到 `%LOCALAPPDATA%\dsh-desktop-shell\harness.log`（harness 是隐藏拉起的，此前启动失败没有任何线索）；退出时按 `tasklist` 核对进程名，避免 pid 复用误杀。

## 0.2.9 (2026-09-03)

- **捆绑桌面客户端升级**（bin/dsh-desktop-shell-client.exe → 0.2.0）：
  - 关闭主窗口行为按设置页三选项生效（退出到托盘 / 每次询问 / 直接退出）。**修复**：此前三个选项均不生效——拦截方式改为 Wails `RegisterHook` + `event.Cancel()`（与官方 hide-window 示例一致），否则框架内置的无条件关闭 listener 总会销毁窗口；
  - 托盘新增「开机自启」勾选（读写 HKCU Run，与插件设置页开关同一条值）；
  - 退出客户端不再杀 Harness（dsh web 后台保留，可随时重连）；
  - **标准多窗口接入（开放协议）**：客户端向每个托管页面注入 `window.dsh.desktop`（version 1），任何插件调用 `openWindow({url,title,width,height})` / `closeWindow(id)` 即可让客户端开原生独立窗口；底层走 WebView2 宿主消息（`window.chrome.webview.postMessage` → `{type:'dsh.desktop.openWindow',...}`），无跨端口 fetch/CORS，任何页面（含外部 harness）都成立；宿主侧由 `RawMessageHandler` 接收。保留旧 `__DSH_DESKTOP_API__` HTTP 桥接兼容；安装 dsh-deepseek-chat（0.3.3 接入该 SDK）后「网页对话」可多窗；
  - 正式版禁用 WebView2 原生右键菜单（DefaultContextMenuDisabled）。

## 0.2.8 (2026-09-02)

- **scope 迁移**：包名 `@coldcgh/dsh-desktop-shell` → `@zerorigin-studio/dsh-desktop-shell`（发布至 zerorigin-studio org）；内部 module id / bundle patch / author 同步更新。code/行为不变。
